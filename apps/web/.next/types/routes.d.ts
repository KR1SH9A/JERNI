// This file is generated automatically by Next.js
// Do not edit this file manually

type AppRoutes = "/" | "/dashboard" | "/dashboard/journeys/[id]/edit" | "/dashboard/journeys/new" | "/journeys/[id]"
type AppRouteHandlerRoutes = "/api/journeys" | "/api/journeys/[id]" | "/api/journeys/[id]/archive" | "/api/journeys/[id]/likes" | "/api/journeys/[id]/memberships" | "/api/journeys/[id]/stats" | "/api/journeys/[id]/tasks/[taskId]/complete" | "/api/journeys/mine"
type PageRoutes = never
type LayoutRoutes = "/"
type RedirectRoutes = never
type RewriteRoutes = never
type Routes = AppRoutes | PageRoutes | LayoutRoutes | RedirectRoutes | RewriteRoutes | AppRouteHandlerRoutes


interface ParamMap {
  "/": {}
  "/api/journeys": {}
  "/api/journeys/[id]": { "id": string; }
  "/api/journeys/[id]/archive": { "id": string; }
  "/api/journeys/[id]/likes": { "id": string; }
  "/api/journeys/[id]/memberships": { "id": string; }
  "/api/journeys/[id]/stats": { "id": string; }
  "/api/journeys/[id]/tasks/[taskId]/complete": { "id": string; "taskId": string; }
  "/api/journeys/mine": {}
  "/dashboard": {}
  "/dashboard/journeys/[id]/edit": { "id": string; }
  "/dashboard/journeys/new": {}
  "/journeys/[id]": { "id": string; }
}


export type ParamsOf<Route extends Routes> = ParamMap[Route]

interface LayoutSlotMap {
  "/": never
}


export type { AppRoutes, PageRoutes, LayoutRoutes, RedirectRoutes, RewriteRoutes, ParamMap, AppRouteHandlerRoutes }

declare global {
  /**
   * Props for Next.js App Router page components
   * @example
   * ```tsx
   * export default function Page(props: PageProps<'/blog/[slug]'>) {
   *   const { slug } = await props.params
   *   return <div>Blog post: {slug}</div>
   * }
   * ```
   */
  interface PageProps<AppRoute extends AppRoutes> {
    params: Promise<ParamMap[AppRoute]>
    searchParams: Promise<Record<string, string | string[] | undefined>>
  }

  /**
   * Props for Next.js App Router layout components
   * @example
   * ```tsx
   * export default function Layout(props: LayoutProps<'/dashboard'>) {
   *   return <div>{props.children}</div>
   * }
   * ```
   */
  type LayoutProps<LayoutRoute extends LayoutRoutes> = {
    params: Promise<ParamMap[LayoutRoute]>
    children: React.ReactNode
  } & {
    [K in LayoutSlotMap[LayoutRoute]]: React.ReactNode
  }

  /**
   * Context for Next.js App Router route handlers
   * @example
   * ```tsx
   * export async function GET(request: NextRequest, context: RouteContext<'/api/users/[id]'>) {
   *   const { id } = await context.params
   *   return Response.json({ id })
   * }
   * ```
   */
  interface RouteContext<AppRouteHandlerRoute extends AppRouteHandlerRoutes> {
    params: Promise<ParamMap[AppRouteHandlerRoute]>
  }
}
